Though, I am set to call different tools, but LLM gives output as json, but my tool looks for plain text.

Tried updating tool description, For first call, it worked but next call again json passed as input.

For now, I updated my tool to adjust with input in case it is coming as json.
